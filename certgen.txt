# Generate a private key
openssl genrsa -out elk-stack.key 2048

# Create a certificate signing request (CSR)
openssl req -new -key elk-stack.key -out elk-stack.csr

# Generate a self-signed certificate
openssl x509 -req -days 365 -in elk-stack.csr -signkey elk-stack.key -out elk-stack.crt





